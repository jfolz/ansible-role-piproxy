address: {{ address }}
cache_path: {{ cache_path }}
cache_size: {{ cache_size }}
read_size: {{ read_size }}
log_level: {{ log_level }}
pingora:
  pid_file: /run/{{ service }}/{{ service }}.pid
  upgrade_sock: /run/{{ service }}/{{ service }}_upgrade.sock
  {{ pingora | to_nice_yaml(indent=2) | indent(2) }}
